defmodule EBoss do
  @moduledoc """
  EBoss is the core backend boundary for the umbrella.

  It owns the Ash domains, persistence layer, and shared backend services
  that power the web application.
  """
end
